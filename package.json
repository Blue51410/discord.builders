{
    "name": "builders",
    "private": true,
    "scripts": {
        "build": "cd components-sdk && yarn build && cd .. && cd website && yarn build",
        "dev": "cd website && yarn dev",
        "deploy": "cd website && yarn deploy"
    },
    "workspaces": {
        "packages": [
            "components-sdk",
            "website"
        ]
    },
    "devDependencies": {
        "prettier": "2.5.1",
        "pretty-quick": "3.1.3"
    },
    "packageManager": "yarn@4.10.3"
}
